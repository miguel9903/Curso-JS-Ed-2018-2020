// Bucles for of y for in

let names = ['Juan', 'Julian', 'Paco', 'Paula', 'Jose'];

// Bucle for of
for (let name of names) {
    console.log(name);
    //console.log(names.indexOf(name));
}

// Bucle for in
for (let index in names) {
    console.log(index);
}